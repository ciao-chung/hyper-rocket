const chalk = require('chalk')
require('../../kernel.js')

module.exports = async function (opts) {
  // greeting
  console.log(chalk.keyword('orange')('🚀 HYPER ROCKET is launching now'))
  console.log(chalk.keyword('orange')('🔥 Power by Ciao Chung'))
}
